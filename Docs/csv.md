# CSV import / export (planned)

Columns (UTF-8, header required):

`name,producer,region,country,color,varietal,vintage,quantity,price_chf,bin,status,notes,updated_at`

Skip bad rows with a summary. Full implementation: milestone M3.
